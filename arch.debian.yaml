
amd64:
  TARGET_ARCH_TAG: amd64
  QEMU_ARCH:
  S6_ARCH: amd64
  QEMU_PREFIX: amd64
  PLATFORM: linux/amd64
  JRE_ARCH: x86_64

arm64v8:
  TARGET_ARCH_TAG: arm64v8
  EXTRA_ARCH_TAGS: arm64
  QEMU_ARCH: aarch64
  S6_ARCH: aarch64
  QEMU_PREFIX: arm64v8
  PLATFORM: linux/arm64/v8
  JRE_ARCH: aarch64

armv7:
  TARGET_ARCH_TAG: armv7
  EXTRA_ARCH_TAGS: armhf
  QEMU_ARCH: arm
  S6_ARCH: armhf
  QEMU_PREFIX: arm32v7
  PLATFORM: linux/arm/v7
  JRE_ARCH: armv7l

ppc64le:
  TARGET_ARCH_TAG: ppc64le
  QEMU_ARCH: ppc64le
  S6_ARCH: ppc64le
  QEMU_PREFIX: ppc64le
  PLATFORM: linux/ppc64le
  JRE_ARCH: ppc64le

s390x:
  TARGET_ARCH_TAG: s390x
  QEMU_ARCH: s390x
  S6_ARCH:
  QEMU_PREFIX: s390x
  PLATFORM: linux/s390x
  JRE_ARCH: s390x
